<div class="show">
    <div class="col-md-offset-3 col-md-6">
        <div class="jumbotron">
            <p>
              <%= @pin.description %>
              <small><%= @pin.created_at.strftime("%a, %d %b") %></small>
            </p>
            <p>
                <%= @pin.user.email %>
            </p>
        </div>


    </div>
</div>

<% if current_user == @pin.user %>
<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>

